{
	"name": "gensome-academy",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^4000.0.0-alpha.25"
	},
	"devDependencies": {
		"@types/node": "^25.0.3",
		"vite": "^7.3.0"
	}
}
